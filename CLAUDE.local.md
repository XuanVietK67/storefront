# CLAUDE.local.md

Personal overrides — gitignored, not committed.

## Dev preferences

- Prefer opening `src/views/PersonalizerView.vue` as the entry point when exploring unfamiliar behaviour.
- When debugging canvas state, start with `src/composables/useCanvas.ts` before touching components.

## Local tool permissions

- Allow running `npm run dev` without confirmation.
